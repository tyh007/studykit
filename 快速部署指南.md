# 🚀 StudyKit 生产部署 — 快速指南

**状态**: ✅ 所有自动配置已完成  
**日期**: 14 May 2026  
**版本**: 0.1.0

---

## ✅ 已自动完成的任务

以下任务**已经自动为你完成**，无需任何操作：

```
✅ 生成安全凭证
   • DB_PASSWORD: +PQslJlF/6Nw3j2JErwZftxBKBmLUx5VJ/yaIOItAkA= (44字符)
   • JWT_SECRET: GIYpK80TMZF8m/3liD4qGk0mhpeqypszrkt3zn0h2jLjnCvf2RihFTr9RqsX7J26 (64字符)

✅ 配置后端安全
   • CORS 已设置为生产模式
   • 速率限制已启用 (100 请求/15分钟)
   • nginx 安全头已配置

✅ 创建部署文档
   • README_PRODUCTION.md
   • PRODUCTION_SETUP.md
   • PRODUCTION_CHECKLIST.md

✅ 项目验证
   • .env 文件受保护
   • Docker 配置就绪
   • 所有文件完整
```

---

## 📋 你还需要做的事情（仅 3 步）

### 第 1 步：保存凭证到密码管理器 ⏱️ 2 分钟

**非常重要！** 请保存以下信息到你的密码管理器（1Password、Bitwarden 等）：

| 项目 | 值 |
|------|-----|
| **数据库密码** | `+PQslJlF/6Nw3j2JErwZftxBKBmLUx5VJ/yaIOItAkA=` |
| **JWT 密钥** | `GIYpK80TMZF8m/3liD4qGk0mhpeqypszrkt3zn0h2jLjnCvf2RihFTr9RqsX7J26` |
| **用途** | StudyKit 生产部署 |
| **生成日期** | 14 May 2026 |

⚠️ **如果丢失这些凭证：**
- 数据库密码丢失 → 需要在 Docker 中重置密码
- JWT 密钥丢失 → 所有用户需要重新登录

---

### 第 2 步：配置 NAS IP 地址 ⏱️ 2 分钟

**情况 1：本地网络部署（推荐，最简单）**

编辑 `.env` 文件，取消注释并设置 CORS_ORIGIN：

```bash
# 在你的笔记本上，运行：
nano .env

# 找到这行并修改成你的 NAS IP（例如 192.168.1.100）
CORS_ORIGIN=http://192.168.1.100
```

**情况 2：使用域名部署（高级）**

```env
CORS_ORIGIN=https://studykit.yourdomain.com
```

**情况 3：跳过此步骤（本地测试）**

如果你只是想本地测试，可以跳过这步。

---

### 第 3 步：部署到 NAS ⏱️ 10 分钟

选择以下方式之一：

#### 🟢 方式 A：自动部署脚本（推荐）

```bash
# 在你的笔记本上，进入项目目录
cd /path/to/studykit

# 运行部署脚本
./deploy.sh --nas-ip 192.168.1.100
```

脚本会自动：
1. 检查本地环境
2. 连接到 NAS
3. 上传代码
4. 启动 Docker 服务
5. 验证部署成功

**如果脚本不工作**，使用方式 B。

---

#### 🟠 方式 B：手动部署（如果脚本失败）

**步骤 B1：SSH 连接到 NAS**

```bash
# 在你的笔记本终端
ssh admin@192.168.1.100

# 输入 NAS 的 SSH 密码
```

**步骤 B2：创建项目目录**

```bash
# 在 NAS 上运行
mkdir -p /volume1/docker/studykit/{postgres,uploads,exports,backups}
exit
```

**步骤 B3：上传代码**

```bash
# 回到你的笔记本，从 studykit 项目目录
cd /path/to/studykit
scp -r . admin@192.168.1.100:/volume1/docker/studykit/
```

**步骤 B4：启动服务**

```bash
# 再次 SSH 到 NAS
ssh admin@192.168.1.100

# 进入项目目录
cd /volume1/docker/studykit

# 启动所有服务
NAS_DATA_DIR=/volume1/docker/studykit docker compose \
  -f docker-compose.yml \
  -f docker-compose.nas.yml \
  up -d --build

# 等待 30 秒
sleep 30

# 检查服务状态
docker compose ps

# 应该看到：
# studykit-db-1       ✓ Up (healthy)
# studykit-backend-1  ✓ Up
# studykit-frontend-1 ✓ Up
```

---

## ✨ 完成！现在可以使用 StudyKit

### 访问方式

在浏览器中打开：

```
http://192.168.1.100
```

（把 `192.168.1.100` 替换成你的 NAS IP）

### 第一次使用

1. 点击 **Sign up** 创建账户
2. 输入邮箱和密码
3. 创建一个 **Module**（课程）
4. 创建一个 **Lecture**（讲座）
5. 上传 PDF 幻灯片
6. 开始记笔记！

---

## 🔍 验证部署成功

### 方式 1：访问网页

打开 http://192.168.1.100，看到登录页面就表示成功 ✓

### 方式 2：测试 API（可选）

```bash
# 检查后端是否运行
curl http://192.168.1.100:3001/api/health

# 如果看到以下内容，表示成功：
# {"status":"ok","version":"0.1.0","db":"connected"}
```

### 方式 3：查看日志（故障排除）

```bash
# SSH 到 NAS
ssh admin@192.168.1.100
cd /volume1/docker/studykit

# 查看日志
docker compose logs -f

# 按 Ctrl+C 退出
```

---

## ⚠️ 如果出现问题

### 问题 1：无法连接到 NAS

```bash
# 确认 NAS IP 正确
ping 192.168.1.100

# 确认 SSH 可用
ssh admin@192.168.1.100 "echo connected"
```

### 问题 2：部署后看不到页面

```bash
# 检查 nginx 是否运行
ssh admin@192.168.1.100
cd /volume1/docker/studykit
docker compose logs frontend

# 检查 80 端口是否开放
curl -v http://192.168.1.100
```

### 问题 3：后端连接错误

```bash
# 检查后端日志
docker compose logs backend

# 检查数据库
docker compose logs db
```

---

## 📚 需要更多帮助？

| 问题 | 查看文档 |
|------|--------|
| 详细部署步骤 | [PRODUCTION_SETUP.md](PRODUCTION_SETUP.md) |
| 安全配置 | [PRODUCTION_CHECKLIST.md](PRODUCTION_CHECKLIST.md) |
| 架构理解 | [DEPLOYMENT.md](DEPLOYMENT.md) |
| 故障排除 | [DEPLOYMENT.md](DEPLOYMENT.md) 第 9 节 |

---

## 🎯 后续可选配置

### 1️⃣ 配置自动备份（强烈推荐）

```bash
# SSH 到 NAS
ssh admin@192.168.1.100
crontab -e

# 添加这一行（每天凌晨 3 点备份）
0 3 * * * cd /volume1/docker/studykit && docker compose exec -T db pg_dump -U studykit studykit > backups/studykit_$(date +\%Y\%m\%d).sql && find backups -name "*.sql" -mtime +30 -delete
```

### 2️⃣ 配置 HTTPS（如果暴露到互联网）

在 NAS 的 **Application Portal** → **Reverse Proxy** 中配置 SSL 证书

### 3️⃣ 配置防火墙

在 NAS 的 **Settings** → **Firewall** 中：
- ✅ 允许端口 80（HTTP）
- ✅ 允许端口 443（HTTPS）
- ❌ 关闭端口 5432（数据库）
- ❌ 关闭端口 3001（后端）

---

## 📊 一键总结

```
已完成 ✅          还需要你做 📋
────────────────────────────────────
✅ 凭证生成         📋 保存凭证（2分钟）
✅ 配置安全         📋 配置 NAS IP（2分钟）
✅ 创建文档         📋 运行部署（10分钟）
✅ 验证项目         ✅ 完成！
                    
总计：约 14 分钟
```

---

## 🚀 现在就开始吧！

```bash
# 1. 进入项目目录
cd /path/to/studykit

# 2. 确保凭证已保存到密码管理器 ✓

# 3. 编辑 .env（可选但推荐）
nano .env

# 4. 运行部署
./deploy.sh --nas-ip 192.168.1.100

# 5. 等待完成（~5分钟）

# 6. 打开浏览器访问
# http://192.168.1.100

# ✨ 完成！开始使用 StudyKit！
```

---

**问题？** 查看 [PRODUCTION_SETUP.md](PRODUCTION_SETUP.md) 获取更多帮助

**更新？** 2026-05-14 版本 0.1.0
