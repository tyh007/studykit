# 🚀 StudyKit 一键部署指南

> **以后每次部署只需要一条命令：** `bash deploy.sh`

---

## ⚡ 快速部署（日常更新）

写完了代码想重新部署？直接跑：

```bash
bash deploy.sh
```

脚本自动完成：检查配置 → 构建镜像 → 重启容器 → 验证运行。

> 数据存在 Docker 数据卷里，**不会丢** ✅

---

## ❓ 核心问题：换电脑/换服务器，数据会丢吗？

### ✅ 答案是：会！

```
数据存在这里：/var/lib/docker/volumes/studykit_*
              ↑↑↑ 这台电脑本地的 Docker 里
```

| 操作 | 数据会丢吗 |
|------|:----------:|
| 同电脑运行 `bash deploy.sh` | ❌ **不会** — 只是重启容器 |
| 同电脑运行 `docker compose down` | ❌ **不会** — 只是停止容器 |
| **换到新电脑** | ✅ **会丢！** — 新电脑没有旧数据 |
| `docker compose down -v`（⚠ 危险） | ✅ **会丢！** — 数据卷被删了 |

### 关键理解

```
┌──────────────────────────────────┐
│  代码 = 项目文件夹 → 可以到处复制 │
│  数据 = Docker 数据卷 → 在这台电脑 │
│                                  │
│  换电脑 = 数据不会自动跟着走！    │
│  必须手动备份和恢复               │
└──────────────────────────────────┘
```

---

## 📤 如何迁移数据到新服务器

### 方法一：数据库备份（推荐，最简单）

**第1步：在老电脑上备份**

```bash
cd /path/to/studykit
docker compose exec db pg_dump -U studykit studykit > studykit_backup.sql
```

得到 `studykit_backup.sql` 文件（包含所有用户、课程、笔记）。

**第2步：复制到新电脑**

```bash
# scp / U盘 / 网盘 都行
scp studykit_backup.sql user@新电脑:/path/to/studykit/
```

**第3步：在新电脑上部署并恢复**

```bash
cd /path/to/studykit

# 1. 部署服务
bash deploy.sh

# 2. 恢复数据
docker compose exec -T db psql -U studykit studykit < studykit_backup.sql
```

### 方法二：NAS 部署（最省心，推荐长期用）

如果你有 NAS（网络存储），把数据存在 NAS 上，**换电脑也不影响**：

```bash
# 在 NAS 上部署（数据存在 NAS 的 /volume1/docker/studykit/ 下）
NAS_DATA_DIR=/volume1/docker/studykit docker compose \
  -f docker-compose.yml \
  -f docker-compose.nas.yml \
  up -d
```

以后无论换哪台电脑，浏览器访问 NAS 地址就行：
```
http://NAS的IP:8080
```

---

## 🆕 首次部署（全新环境）

### 步骤 1：把项目复制到新机器

```bash
# 在老电脑上打包（不含 node_modules）
cd /path/to/studykit
tar -czf studykit.tar.gz --exclude='node_modules' --exclude='.env' .

# 复制到新服务器
scp studykit.tar.gz user@新电脑:/path/to/
```

### 步骤 2：解压并配置

```bash
cd /path/to/studykit
tar -xzf studykit.tar.gz

# 创建 .env 配置文件
cp .env.example .env
# 编辑并填写密码
nano .env
```

### 步骤 3：部署

```bash
bash deploy.sh
```

---

## 📋 常用命令

| 操作 | 命令 |
|------|------|
| **一键部署** | `bash deploy.sh` |
| 查看状态 | `docker compose ps` |
| 查看日志 | `docker compose logs -f` |
| 停止服务 | `docker compose down` |
| 重启后端 | `docker compose restart backend` |
| **备份数据库** | `docker compose exec db pg_dump -U studykit studykit > backup.sql` |
| **恢复数据库** | `docker compose exec -T db psql -U studykit studykit < backup.sql` |
| 进入数据库 | `docker compose exec db psql -U studykit -d studykit` |
| 查看数据卷 | `docker volume ls \| grep studykit` |

---

## ⚠️ 危险操作提醒

```bash
# ❌ 这会删除所有数据！
docker compose down -v

# ✅ 安全停止（保留数据）
docker compose down
```

---

## 📁 项目文件

| 文件 | 作用 |
|------|------|
| `deploy.sh` | 🎯 **一键部署脚本** |
| `docker-compose.yml` | Docker 服务配置 |
| `docker-compose.nas.yml` | NAS 数据持久化配置 |
| `.env` | 密码和密钥 |
| `backend/` | 后端代码 |
| `frontend/` | 前端代码 |

